# Oracle Free VM deployment env (copy to /opt/eapproval/.env)
# Google OAuth / Picker
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_API_KEY=
GOOGLE_CLOUD_APP_ID=
GOOGLE_DRIVE_SCOPE=https://www.googleapis.com/auth/drive.file https://www.googleapis.com/auth/documents.readonly

# Optional legacy fallback for Google auth login button
GOOGLE_CLIENT_ID=
