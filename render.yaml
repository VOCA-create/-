services:
  - type: web
    name: 전자결재
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py --host 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
